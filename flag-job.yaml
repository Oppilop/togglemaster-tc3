apiVersion: batch/v1
kind: Job
metadata:
  name: flag-postgres-init
spec:
  template:
    spec:
      restartPolicy: Never
      containers:
      - name: flag-postgres-init-sql
        image: postgres:latest
        command: ["psql"]
        args: ["-h", "postgres-flag.cpuaso8oc4w8.us-east-1.rds.amazonaws.com", "-U", "postgres", "-d", "flagdb", "-f", "/mnt/init.sql"]
        env:
        - name: PGPASSWORD
          valueFrom:
            secretKeyRef:
              name: flag-postgres-init-secret
              key: DB_PASSWORD
        volumeMounts:
        - name: init-sql
          mountPath: /mnt
      volumes:
      - name: init-sql
        configMap:
          name: flag-postgres-init-configmap
---
apiVersion: v1
kind: Secret
metadata:
  name: flag-postgres-init-secret
type: Opaque
data:
  DB_PASSWORD: N0lURmJsLlpxX1sxKDJUKkg5ZW1+LWlNOjdiOg==
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: flag-postgres-init-configmap
data:
  init.sql: |
    CREATE TABLE IF NOT EXISTS flags (
    id SERIAL PRIMARY KEY,

    -- 'name' é a chave de negócio única (ex: 'enable-new-checkout')
    name VARCHAR(100) UNIQUE NOT NULL, 
    description TEXT,
    -- Este é o 'kill switch' global. Se for false, a flag está desativada para todos.
    is_enabled BOOLEAN NOT NULL DEFAULT false,
    created_at TIMESTAMP WITH TIME ZONE DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP WITH TIME ZONE DEFAULT CURRENT_TIMESTAMP
    );
    -- Opcional, mas boa prática: Trigger para atualizar 'updated_at' automaticamente
    CREATE OR REPLACE FUNCTION trigger_set_timestamp()
    RETURNS TRIGGER AS $$
    BEGIN
      NEW.updated_at = NOW();
      RETURN NEW;
    END;
    $$ LANGUAGE plpgsql;

    -- Remove o trigger se já existir, para evitar erro na recriação
    DROP TRIGGER IF EXISTS set_timestamp ON flags;

    CREATE TRIGGER set_timestamp
    BEFORE UPDATE ON flags
    FOR EACH ROW
    EXECUTE PROCEDURE trigger_set_timestamp();
